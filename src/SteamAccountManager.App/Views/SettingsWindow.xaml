<ui:FluentWindow x:Class="SteamAccountManager.App.Views.SettingsWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Settings"
                 Width="420" Height="300"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:TitleBar Grid.Row="0" Title="Settings" />
        <StackPanel Grid.Row="1" Margin="24">
            <ui:ToggleSwitch Content="Start with Windows"
                             IsChecked="{Binding AutostartEnabled, Mode=TwoWay}" Margin="0,0,0,16" />
            <ui:ToggleSwitch Content="Start minimized to tray"
                             IsChecked="{Binding StartMinimized, Mode=TwoWay}" Margin="0,0,0,16" />
            <ui:ToggleSwitch Content="Check for updates on startup"
                             IsChecked="{Binding CheckForUpdatesOnStartup, Mode=TwoWay}" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
